# Progress Journal

Human-readable mirror of `game/state/progress.json`. The Game Master updates this
at the end of every session (Law 3).

## Learner profile (recalibrated 2026-07-18)

**Dimitris** — hero name `dimitris`. Frontend engineer (~4 yrs JavaScript:
vanilla JS → React/Next.js → agency work, light Node/Express) transitioning into
AI engineering. Strong on AI theory and product sense; a "vibe coder" who has
shipped **no Python and no ML/AI code** yet.

**Calibration:** his gap is doubled — Python-from-blank-page *and*
backend/systems/AI-implementation — while his CS fundamentals and AI theory are
solid. Bridge every Python/AI concept from its JavaScript equivalent, keep the
conceptual/Decision-Card depth high, scaffold Python *syntax* (never the quest
solution). Rune Plains Python fluency is the critical path. Target: interview-ready
~Aug–Sep 2026.

## Next up

- Play the game: `play.command` (macOS) → do the daily Rune Trial drill. In any
  quiz boss, press **«Ρώτα τον Δάσκαλο»** after a question for a deep, bilingual
  (Greek + interview-English) explanation — a live Gemini call.
- Start at the campfire: **Rune Plains L0 · First Steps** —
  `01-python-fluency/lesson-00-first-steps/LESSON.md` — run your first Python, then
  do the warm-up rite. (Already comfortable with Python basics? Jump to
  **L1 · Runes of Data** and read its new **Section 0 · In plain words** first.)

## Active curses (weak spots)

*None yet — they appear when quiz questions are missed.*

## Session log

### 2026-07-23 — L2 · Flow of Mana authored (beginner-first, playable)
- Second Rune Plains lesson, written end-to-end in the new beginner style:
  `lesson-02-flow-of-mana/LESSON.md` — plain-words Section 0, JS↔Python bridges,
  three Decision Cards (return-vs-mutate, EAFP-vs-LBYL, try/finally-vs-context-
  manager) and a 12-line demo. Covers functions & the silent `None` return,
  `*args`/`**kwargs`, the mutable-default trap, LEGB + `UnboundLocalError`,
  closures/`nonlocal`, try/except/else/finally, `raise`, and context managers.
  Every snippet executed on Python 3.14.
- Quests (blank-file, TEST-ONLY workspaces): Q1 · The Mana Conduits (`conduits.py`)
  and Q2 · The Warded Gate (`wards.py`). I verified the tests are a correct,
  achievable spec against throwaway reference solutions (17 + 15 green), then
  discarded them — the student writes the modules (Law 1).
- Quiz bank `pf-l2.json` — 26 questions (16 predict / 5 bug / 5 truth), boss **The
  Warden of Flow**. Generated by a script that EXECUTES every predict snippet and
  asserts the correct choice equals real stdout, so a wrong "correct answer" is
  impossible by construction; the bug/truth premises are asserted too.
- Wiring is pure data (quests.json entries + the bank) **plus one small engine
  change**: `state.js activeBossForRegion` makes the single per-region Quiz Boss
  button advance L1 → L2 → … (the engine assumed one boss per region; the
  curriculum wants one boss per lesson). Progression logic + all JS syntax verified.
- Gating: L2 quests require the L1 boss; the Warden requires both L2 quests —
  reachable in-game once the Keeper of Runes falls. No save/XP changes.
- Note: the current save has `pf-l1-q1` = `awaiting_review`, but no `word_hoard.py`
  exists in that workspace — likely a stale state to reconcile with the student
  next session (not touched here).

### 2026-07-23 — Beginner recalibration: Level 0 + «Ρώτα τον Δάσκαλο» (Gemini)
- Student asked to lower the on-ramp (frontend→AI; Python is brand new to him) and
  to get deeper, teacher-style explanations on demand rather than a one-line answer.
  Two things shipped:
- **«Ρώτα τον Δάσκαλο» button** in the quiz boss — a live Gemini call that explains
  any question/answer teacher-to-student: bilingual (Greek explanation + a short
  "How you'd say this in an interview" in English), always bridging from JavaScript.
  Key stays server-side: `game/secrets.local.json` (gitignored) → FastAPI
  `POST /api/explain` (`game/server/gemini.py`, stdlib-only, written to double as a
  Great Library L1 reference) → Gemini `gemini-flash-latest`. Added `certifi` for
  macOS SSL. Tested end-to-end with a real call (Greek + interview block, JS bridge).
- **Level 0 · First Steps** (`01-python-fluency/lesson-00-first-steps/`): a narrative,
  JS-bridged campfire before the runes — running code, variables/types, print &
  f-strings, if/for/while, reading tracebacks, plus a self-run warm-up rite. No boss.
  Every snippet executed on Python 3.14 before shipping.
- **L1 additive layer:** new "Section 0 · In plain words" (the four containers via JS
  analogies + a which-to-reach-for table) prepended to Runes of Data; the senior
  sections are untouched. Fixed the stale "you already know Python" framing in
  TRACK.md (wrong for this learner). Added L0 to CURRICULUM.md and TRACK.md tables.
- Approach the student chose: additive beginner layer **and** a separate Level 0,
  with JS↔Python analogies throughout.
- **Rollout (student-approved):** added a "🌉 From your frontend life" JS bridge +
  Δάσκαλος companion note to all four sealed region overviews — the Gatekeeper's
  Forge (Node/Express→FastAPI), the Great Library (REST→LLM APIs), the War Council
  (frontend trade-offs→system design), the Dragon's Keep (agency take-homes→
  capstones) — matching the track-01 treatment.
- **Still to author, beginner-first (the real gap):** L2–L6 of the Rune Plains and
  every lesson in tracks 02–05 exist only as CURRICULUM/TRACK rows — the game's
  bosses/trials are built, but the LESSON.md study material, quests, and per-lesson
  quiz banks are not written. There is nothing yet to "layer onto" there; they get
  written beginner-first when authored (one lesson per session to keep quality —
  each needs a verified quiz bank + quests + tests). **Recommended next:
  L2 · Flow of Mana.** No XP/save changes this session.

### 2026-07-18 — Recalibration to the actual learner (Dimitris)
- Original game was authored (by `GiannisKarna`) assuming a Python-fluent student;
  actual learner is a frontend/JS engineer moving into AI eng. Inspected GitHub
  (`DBelwnhs`) to ground the profile: React/Next, vanilla JS fundamentals, some
  Node/Express, CSS/UI — no Python, no ML/AI shipped.
- Recalibrated the student profile in `CLAUDE.md` and added the learner-profile
  block above. Teaching adjustment: bridge from JavaScript, keep theory deep,
  scaffold Python syntax without solving quests. Personal calibration also stored
  in the Game Master's project memory.
- Added macOS launchers (`play.command`, `play.sh`) + README setup for mac/Linux.
- Done on branch `dbelwnhs/calibrate-learner-profile`.

### 2026-07-18 — THE GAME IS FINISHED (Phases 2+3 shipped)
- Every region now has its Region Trial: The Tavern of One Bartender (async/event
  loop, Forge), The Sundered Scrolls (RAG, Library), The Summoning Circles
  (agents/orchestration, Library), The Council of Trade-offs (Decision-Card
  battles, War Council), and Veldrath the Dragon (final 3-phase gauntlet, Keep).
- Curse Codex in the HUD, WebAudio SFX with mute, engine-owned Esc-abort,
  grand-victory celebration. All content adversarially fact-checked by verifier
  agents; integrator fixed one abort-cleanup leak across all seven minigames.
- Full-game Playwright playtest: 19/19 checks, zero console/page errors, one
  trial driven to completion end-to-end; save restored to pristine (now includes
  "trials": {}).
- Regions beyond the Rune Plains remain sealed for the student — the Game
  Master unlocks each when the previous region's P0 lessons are passed.

### 2026-07-18 — Visual playtest (Game Master, no student play)
- Full Playwright playthrough in real Chromium: naming, WASD/arrow movement,
  landmark trigger + Enter, region panel, boss gating, Begin Quest, complete
  10-question drill (deliberate misses → curses recorded), XP/ember math,
  reload persistence. 16/16 checks passed, zero console/page errors.
  Save state restored to pristine — student still starts fresh.

### 2026-07-17 — Session 0 (world creation)
- Designed the classroom: spec in `docs/superpowers/specs/2026-07-17-grand-trial-design.md`.
- Built Phase 1 MVP: game engine, Rune Trials minigame, quiz boss, Lesson 1.
- Student decisions recorded: four-track+capstone structure, game-as-world with
  real-code quest gating, browser+FastAPI tech, fantasy RPG theme, Docker deferred.
- No code written by the student yet — XP starts at 0. The Trial begins.
