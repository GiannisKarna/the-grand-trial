# The Great Library — Track 03 · AI Engineering

> *A tower of scrolls taller than the clouds, tended by spirits that speak in
> probabilities. Every scroll can be found — if you know how meaning is
> measured, how the shelves are chunked, and how to judge whether the spirit's
> answer is wisdom or confident nonsense.*

## 🔒 Sealed

This region unlocks after the Gatekeeper's Forge's P0 lessons (the Game Master
lifts the seal in the save file). Its trials build directly on the Forge:
you will serve what you retrieve.

**What awaits:** raw LLM APIs (chat, streaming, tool use, tokens, pricing),
embeddings and vector search, RAG built raw then hardened for production,
LangChain/LangGraph with full Decision Cards, and evals — the Judging Eye.

> 🌉 **From your frontend life:** you've called REST APIs from React and rendered
> streaming responses — an LLM API is just another HTTP endpoint: messages in,
> tokens out. "Embeddings + vector search" is a smarter *find-similar* (a fuzzy
> search box). RAG is fetch-then-render, except the render step is a model. Your
> AI theory and product sense — your real strengths — lead here; the code is the
> part we scaffold. (The **«Ρώτα τον Δάσκαλο»** button you use in the quiz boss is
> itself a tiny piece of this region — a raw LLM call behind your own server.)

When the seal breaks, these lessons will be authored **beginner-first**, like the
Rune Plains — plain-words intros, JS→Python bridges, and the Δάσκαλος one click
away for a deep Greek explanation.

**Lesson list:** see `CURRICULUM.md` · *03 · The Great Library* — eight
lessons, L1–L4 are P0. Lesson folders will appear here when the seal breaks.
