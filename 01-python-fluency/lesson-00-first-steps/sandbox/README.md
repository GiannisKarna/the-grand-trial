# Sandbox — L0 scratch space

Your playground for L0. Nothing here is graded or committed-for-review — poke,
break, delete freely.

Suggested first file, `hello.py`:

```python
print("The Trial begins.")
name = "Dimitris"
print(f"Welcome, {name}.")
```

Run it from the **repo root**:

```bash
.venv/bin/python 01-python-fluency/lesson-00-first-steps/sandbox/hello.py
```

Then do the **Warm-up rite** (LESSON.md §8) in a second file here. When it prints
the party roll call exactly, L0 is done — move on to
[L1 · Runes of Data](../../lesson-01-runes-of-data/LESSON.md).
